# Neural Network Framework.

This is a, currently very rough, framework to train neural networks in CSharp.
The code is in a rough shape right now, cause I've not programmed in CSharp in years, and the previous codebase was absolutely horrid as I was not a great programmer at that time.
Right now, it uses stochastic gradient descent w/ backpropagation, and has some basic activation functions. The framework's rough edges will be smoothened out, and hiccups fixed.
